export const loadAbout = function(){

    //Remove all children in dic #element:
    const parent = document.querySelector("#content");
    while (element.firstChild) {
        element.removeChild(element.firstChild);
    }
}